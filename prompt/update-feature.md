# 更新功能

给启动器添加更新功能，当我选择到更新页面时，页面提示“请选择更新文件”，下方则是选择按钮，然后弹出窗口让用户选择压缩包。你需要在选择以后，调用包内的7z工具进行解压，解压到当前游戏路径update_tmp下。

然后设置临时环境变量给与更新bat脚本使用，脚本会自动读取这个环境变量然后进行更新。
请同时修改更新脚本，将手动捕获输入改成直接读取环境变量，更新完成后最后不使用pause，而是等待5秒自动退出，并返回特定代码，例如0，启动器会捕获返回的代码，如果为0，则更新成功，如果返回为-1，则更新异常。

每次完成后，都应当清除解压后的更新文件