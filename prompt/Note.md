# 1

读取预配置与读取系统配置的切换有点问题，需要修

逻辑应当默认读取系统配置，修改系统配置，如果预配置功能开启，在修改预配置

之后的逻辑应当会更改为读取系统配置，如果检测不到，则提示是否导入系统配置（在全新配置页面里也会有），导入后即可读取到系统配置

# 2

检测到spice64退出后，等待1秒，如果再次检测到了spice64.exe，则不进行所有程序的退出流程（例如氧无）

# 3 

Mod管理？maybe

# 4

数据迁移
- asphyxia config
- asphyxia savedata
    - 自动备份
- card0，card1

