# 修改内容

1. 移除DisplayConfigure里关于norestorerotation的相关内容。重新设计一个退出游戏时，恢复启动前显示器状态的功能，并将此功能绑定到“退出游戏后恢复显示器布局”的开关上,config里的名称为exitrestore，在[Display]类别下，最终的效果应当是我开启此功能后，如果显示器配置处于启用状态，当游戏退出时，自动还原成启动游戏前显示器的状态。没开启此功能则不还原。

2. 给启动按钮右侧分割出来一小部分，然后放置一个下拉选项，可以选择不同的启动模式。如果直接点击左侧的启动按钮，就是正常启动。如果点开下拉栏，里面应该会有一个以调试模式启动氧无，如果用户点击，此时也会立刻执行启动进程，但是不启动spice64,只启动asphyxia，且会带上--dev的启动参数（即设定选项里的以调试模式启动氧无），用户在设定里启用的不启动氧无不影响此处的功能。实现上述功能以后移除设定选项里的以调试模式启动氧无

3. 将设定选项里的杂项分类重命名为读卡器；调试选项重命名为杂项