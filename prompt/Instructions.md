# 迁移程序版本至.NET 10

当前项目所使用的是.NET Framework 4.7.2+WinForms，我想要将其迁移至.NET 10+WPF

## 要求

1. 不改动当前的界面UI，只将界面框架从WinForms迁移到WPF，迁移后界面UI保持不变，如果出现UI显示异常，需要进行修复
2. 不要大范围的改动代码逻辑，只在必须要迁移至新框架的代码进行改动
3. 支持Native AOT编译
4. 新建全新的Git分支，并在新分支下进行迁移 
